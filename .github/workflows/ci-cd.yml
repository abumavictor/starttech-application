name: CI/CD Pipeline

on:
  push:
    branches:
      - feature/full-stack

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: List files
        run: ls -R
